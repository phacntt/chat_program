import { Menu } from 'antd';
import { styled } from 'styled-components';

export const MenuListRoom = styled(Menu)`
  .ant-menu-item {
    height: 60px;
    .ant-menu-title-content {
      font-size: 16px;
      padding-left: 10px;
    }
  }
`;
